import "../styles/Notes.css";

const Notes = () => {
  return (
    <div className="notes">
      <h2>Studio</h2>
      <button className="add-note">+ Añadir nota</button>
    </div>
  );
};

export default Notes;
